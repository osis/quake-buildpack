#!/usr/bin/env bash
# bin/release <build-dir>

echo -e "---\ndefault_process_types:\n  web: pwd && ls -la && env && sed -i 's/27910/\${PORT}/' ./lgsm/config-default/config-lgsm/q2server/_default.cfg && ./q2server start"
